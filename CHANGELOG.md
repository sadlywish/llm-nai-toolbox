# 更新日志

版本号遵循 `主.次.修订`。0.9.x 均为预发布（内部测试），界面与配置格式还可能调整。下载见 [Releases](https://github.com/sadlywish/llm-nai-toolbox/releases)。

## 0.9.2 — 2026-09-15

### 新增

- **魔法书**：顶栏新增「魔法书」标签，把 LLM 的 browse_tags 工具用的分类数据摊开给人翻——9 组、126 类、1.7 万个标签，每个都带中文释义。
  - 左边分类树，点一类列出这一类全部标签（按帖子数排序），标出「易误读」「辨析」。
  - 右边是选中标签的中文说明（释义、易误读提示、辨析）和 D 站 WIKI；辨析与 wiki 正文里的标签点了直接跳过去。
  - 检索框能搜标签名、中文释义、中英日别名，多个词用空格隔开表示同时命中；分类树只留有命中的分类并显示命中数。
  - 「复制」把标签复制到剪贴板（下划线换成空格）。
- **WIKI 竖栏**：标签词条下显示中文说明卡片；搜索框输入中文时，联想末尾补上释义里包含这个词的标签（标「释义」）。
- **编辑器补全**：下拉每行带中文释义；输入中文时，末尾补上释义里包含这个词的标签（标「释义」，最多 10 条）。

### 改动

- 标签分类的二级分类名全部改为中文（如「头发/发型」「身体/面部与表情」「画面构成/构图与视角」），LLM 看到的分类目录同步变成中文。

## 0.9.1 — 2026-09-15

### 新增

- 画风维护：「覆盖画风」（把一条画风整段写进工作台的 artist 块并切回工作台）、「从工作台提取」（把 artist 块存成新画风）。
- 规范化权重与 @：把 webui 转义的 `\(` `\)` 还原成普通括号。

### 改动

- 设置从侧边抽屉改为和工作台、画风维护并排的标签页：没保存的修改切标签不丢，标签上挂黄点；底部有「撤销修改」「保存」。
- 提示词类输入框（系统提示词、多角色附加、尾部注入、质量词、负面词、画面文字等）按内容撑高，最高约一屏；质量词、负面词留空时高亮提醒。
- 画风维护改为左侧列表 + 右侧详情：拖动排序，绿点标出当前预设画风并在列表顶上置顶；双击名称全选改名。
- 预设画风改在画风维护里「选为预设画风」，指令区只显示当前预设名和「选择预设…」，选预设不改指令区的画风档位。
- LLM 请求不再向用户消息注入画风（回填前直接覆盖 artist）。
- 出图弹窗的溯源信息常驻右侧（默认显示第一张出好的图），点遮罩关闭弹窗。
- 默认系统提示词更新；质量词、负面词加默认值。

## 0.9.0 — 2026-09-15

首个打包版本。

- **分块提示词编辑器**：十个字段常驻，块边界删不穿，空段拼接时跳过；标签补全、权重调整、全角逗号标红。
- **LLM 对话**：指令区固定在窗口底部；多轮查本地标签库（search_tags / browse_tags / 标签手册），回填编辑器、负面词、参数区与角色区；日志抽屉按行显示过程；支持 Claude 与 OpenAI 兼容接口、思维链。
- **画风预设**：用预设画风或当前 artist 块覆盖模型返回的画风；规范化权重与 @。
- **出图**：NovelAI 出图、跑图次数、回填后自动生成；出图弹窗与溯源信息（本工具参数 / 图片元信息 / 复制信息）；429 暂停、Token 或点数问题中止整批；像素上限预设与 Opus 免费范围提示。
- **历史**：左侧历史竖栏按轮次回看。
- **右侧 WIKI**：画师 / 标签两源、跟随光标查询、搜索联想、DText 正文、例图，「加入」插回上次光标位置；可整体收起。
- **打包**：Windows x64 安装版、便携版、zip，标签库随包分发。
